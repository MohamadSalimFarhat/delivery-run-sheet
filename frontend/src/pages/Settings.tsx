export default function Settings() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Settings</h1>
      <p className="mt-2 text-sm text-slate-500">
        Display name and WhatsApp message template arrive in step 10.
      </p>
    </div>
  )
}
